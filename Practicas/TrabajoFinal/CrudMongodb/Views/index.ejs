<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clientes</title>
</head>
<body>
    <h1>Lista de Clientes</h1>
    <a href="/users/new">Crear Nuevo Cliente</a>
    <ul>
        <% users.forEach(user => { %>
            <li>
                <strong><%= user.ci %></strong> - <%= user.nombres%> 
                (<%= user.apellidos %>) 
                <a href="/users/<%= user._id %>">Ver</a>
                <a href="/users/<%= user._id %>/edit">Editar</a>
                <form action="/users/<%= user._id %>?_method=DELETE" method="POST" style="display: inline;">
                    <button type="submit">Eliminar</button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>

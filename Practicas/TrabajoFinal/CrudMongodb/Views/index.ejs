<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clientes</title>
</head>
<body>
    <h1>Lista de Clientes</h1>
    <a href="/Cliente/new">Crear Nuevo Cliente</a>
    <ul>
        <% Cliente.forEach(user => { %>
            <li>
                <strong><%= user.ci %></strong> - <%= user.nombres%> 
                (<%= user.apellidos %>) 
                <a href="/Cliente/<%= user._id %>">Ver</a>
                <a href="/Cliente/<%= user._id %>/edit">Editar</a>
                <form action="/Cliente/<%= user._id %>?_method=DELETE" method="POST" style="display: inline;">
                    <button type="submit">Eliminar</button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>

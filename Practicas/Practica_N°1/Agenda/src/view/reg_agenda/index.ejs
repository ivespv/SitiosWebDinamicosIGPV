<!DOCTYPE html>
<html>
<head>
  <title>Agenda</title>
</head>
<body>
  <h1>Registro de Agenda</h1>
  <a href="crear.ejs">Crear Agenda</a>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>nombres</th>
        <th>apellidos</th>
        <th>direccion</th>
        <th>telefono</th>
      </tr>
    </thead>
    <tbody>
      <% reg_agenda.forEach(agenda => { %>
        <tr>
          <td><%= agenda.id %></td>
          <td><%= agenda.nombres %></td>
          <td><%= agenda.apellidos %></td>
          <td><%= agenda.direccion %></td>
          <td><%= agenda.telefono %></td>
       <td>
            <a href="editar.ejs/<%= agenda.id %>">Editar</a>
            <form action="eliminar.ejs/<%= agenda.id %>" method="POST" style="display:inline;" onsubmit="return confirm('¿Estás seguro de que deseas eliminar este registro?');">
             <button type="submit">Eliminar</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
